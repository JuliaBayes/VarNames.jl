var documenterSearchIndex = {"docs":
[{"category":"section","location":"vnt/motivation/#Motivation","page":"Motivation","text":"The original design for VarNamedTuple came from probabilistic programming with DynamicPPL.jl, and so is described here with that context.\n\nnote: Note\nThe DynamicPPL code samples on this page are not executed.\n\nWhen executing a DynamicPPL model, it is very often necessary to store information about the random variables in the model. Consider, e.g.\n\nusing DynamicPPL, Distributions\n\n@model function f1()\n    x ~ Normal(0, 1)\n    y ~ Normal(x, 1)\n    z ~ Normal(y, 1)\n    return nothing\nend\nnothing # hide\n\nWhen we evaluate the model, we want to know the values of x, y, and z (even if the return value of the function is nothing; the return value is typically used for other purposes). Sometimes we might want to store other information about them, like their distributions. This information can be extracted and used by inference algorithms, for example.\n\nIn general this is done by storing some kind of mapping from VarNames to values. This is the job of VarNamedTuples, or VNT for short. Before we go into that, let's take a short view back to the past to see various approaches that don't quite work. This will provide a motivation for the design of VNT.","title":"Motivation"},{"category":"section","location":"vnt/motivation/#Historical-approaches","page":"Motivation","text":"Julia contains two main data structures for key-value maps: NamedTuples and Dicts.","title":"Historical approaches"},{"category":"section","location":"vnt/motivation/#NamedTuple","page":"Motivation","text":"NamedTuples are extremely lightweight data structures and are completely type stable. For example, in the above, we could store the values in a NamedTuple like so:\n\n(x=0.5, y=1.2, z=-0.3)\nnothing # hide\n\n(or whatever the sampled values are.) The issue with this, however, is that the keys of NamedTuples are symbols. This is fine for variables that are single identifiers (i.e., VarNames with an optic of AbstractPPL.Iden; which we'll call 'identity VarNames').\n\nBut in a model like the following, we have the VarNames x[1], x[2], x[3], and y.a. These cannot be properly converted into Symbols without loss of information, and this makes the output NamedTuple unsuitable for subsequent use.\n\n@model function f2()\n    x = Vector{Float64}(undef, 3)\n    for i in eachindex(x)\n        x[i] ~ Normal()\n    end\n\n    y = (; a=1.0)\n    return y.a ~ Normal()\nend\n\nIn the past, there was something called SimpleVarInfo which stored variables in NamedTuples (possibly converting non-identity VarNames to symbols with loss of information). This was a very fast data structure, but was unsuitable for general use.","title":"NamedTuple"},{"category":"section","location":"vnt/motivation/#Dict","page":"Motivation","text":"In order to be completely general, we could use a Dict{VarName, Any}. This allows us to store arbitrary VarNames as keys, and arbitrary values. Again, there was a SimpleVarInfo implementation that used this approach (specifically an OrderedDict).\n\nusing VarNames\n\nDict(\n    @vn(x[1]) => 0.5,\n    @vn(x[2]) => -1.2,\n    @vn(x[3]) => 1.7,\n    @vn(y.a) => 0.3,\n)\n\nThe main issue with Dicts is performance. In general there is substantial overhead in hashing and looking up keys in a Dict, compared to NamedTuple field access which is just direct memory access. Furthermore, accessing values is type-unstable (unless all the values can be made to share the same concrete type, which is the case above, but is unrealistic).","title":"Dict"},{"category":"section","location":"vnt/motivation/#Metadata-and-VarNamedVector","page":"Motivation","text":"In an attempt to get around the performance issue, DynamicPPL used to have data structures called Metadata and VarNamedVector. These structures essentially attempted to remove as much overhead as possible from Dicts by flattening their elements (i.e. variable values) and storing vectorised forms of them in contiguous vectors. Furthermore, they were often wrapped in NamedTuples to get some type stability benefits. This approach was somewhat successful and was used for many years: it is also described in the Turing.jl ACM Transactions on Probabilistic Machine Learning paper.\n\nWhile this fixed many performance problems with Dict, at its very core, it is still a mapping of VarNames to values, and therefore suffers from a lack of what we call 'constructiveness'.","title":"Metadata and VarNamedVector"},{"category":"section","location":"vnt/motivation/#constructiveness","page":"Motivation","text":"This is a very subtle issue, but caused a number of headaches with DynamicPPL. As we will soon see, this is the main motivation behind VNT, and even so VNT does not fully solve it.\n\nConsider the following model:\n\n@model function f3()\n    x = Vector{Float64}(undef, 2)\n    x[1] ~ Normal()\n    return x[2] ~ Normal()\nend\n\nIf at the end of the model we were to ask what the value of x was, we would have no way of knowing this. This is what we refer to as a lack of constructiveness: it is not possible to reconstruct the value of x from the stored information.\n\nLet's say we are using a Dict to store the values. We would have something like\n\nd = Dict(@vn(x[1]) => 0.5, @vn(x[2]) => -1.2)\n\nbut attempting to get d[@vn(x)] would return nothing, since there is no such key in the Dict. In fact, we cannot even access x[1:2], despite both indices obviously being defined.\n\nhaskey(d, @vn(x)), haskey(d, @vn(x[1:2]))","title":"Constructiveness"},{"category":"section","location":"vnt/motivation/#What's-the-problem-with-this?","page":"Motivation","text":"One problem that a lack of constructiveness caused was that conditioning on values was unusually finicky. In such cases, you had to condition on x in full, rather than its components:\n\nmodel = f4() | Dict(@vn(x) => [1.0, -1.0])  # This would work.\nmodel = f4() | Dict(@vn(x[1]) => 1.0, @vn(x[2]) => -1.0)  # This would not.\n\nOn top of that, we are unable to properly use different indexing schemes. For example, x[1] and x[1,1] mean the same thing if x is a Matrix, but the dictionary will think that they are different VarNames. Fundamentally, the issue is that the indexing semantics inside a Dict are not the same as the indexing semantics inside the model itself.\n\nFinally, historically, reading information back from a Chains object also proved to be very difficult. For example, MCMCChains.jl breaks up all variables into its constituent scalar components. Thus, even if you have a multivariate distribution\n\nusing LinearAlgebra\n\n@model function f4()\n    x ~ MvNormal(zeros(2), I)\n    return x\nend\nnothing # hide\n\nsampling from this model would give a chain with keys x[1] and x[2]. When calling functions such as returned or predict on the chain, we have to somehow 'reconstruct' the value of x from its components, so that when executing the model we can use the actual value of x. (The more modern FlexiChains.jl largely solves this problem by not breaking up the variables prematurely.)","title":"What's the problem with this?"},{"category":"section","location":"vnt/motivation/#Desiderata","page":"Motivation","text":"In summary, we want a data structure that:\n\nCan store arbitrary VarNames as keys, with arbitrary values.\nIs performant and as type stable where possible. In particular, property access should always be type-stable, and indexing should be type-stable as long as the indexed values are homogeneous in type.\nIs constructive.\n\nVarNamedTuple generally solves these issues, with a few very niche edge cases that will be discussed at the very end.","title":"Desiderata"},{"category":"section","location":"vnt/arraylikeblocks/#array-like-blocks","page":"Array-like blocks","text":"In a number of VNT use cases, it is necessary to associate multiple indices in a VarNamedTuple with an object that is not necessarily the same number of elements.\n\nConsider, for example, a case where x[1:3] has a Dirichlet prior. With a Dict{VarName,Distribution}, we can do this:\n\nnote: Note\nWe'll make a 'fake' Dirichlet struct here to avoid importing Distributions.\n\nusing VarNames\n\nstruct Dirichlet{T}\n    α::Vector{T}\nend\n\nd = Dict{VarName,Dirichlet}(@vn(x[1:3]) => Dirichlet(ones(3)))\n\nbut we incur all the costs associated with the use of a Dict, as described before.\n\nWith a VarNamedTuple, we cannot store this directly:\n\nvnt.data.x = some_array\nvnt.data.x[1:3] = Dirichlet(ones(3))  # will error\n\nbecause Dirichlet is not an array, and setindex! will fail. Nor can we write\n\nvnt.data.x = some_array\nvnt.data.x[1:3] .= Dirichlet(ones(3))\n\nbecause although this will not error, it is semantically different: this means that every element x[1], x[2], and x[3] will be assigned the same Dirichlet(ones(3)) object, which is not what we want.\n\nThe current solution to this is to use ArrayLikeBlocks, which are thin wrappers around the actual value, but additionally also store the indices used to set the value. The second and third arguments here are the indices (positional and keyword) used to set the value, and the fourth argument is the size of the block.\n\nusing VarNames: ArrayLikeBlock\n\nalb = ArrayLikeBlock(Dirichlet(ones(3)), 1:3, (;), (3,))\n\nWe then set this ArrayLikeBlock in all the relevant indices. The extra information in the ArrayLikeBlock allows us to forbid partial indexing into it later on. In particular, we want to ensure that users can only retrieve the entire block at once, and not e.g. just x[1] or x[2:3].","title":"Array-like blocks"},{"category":"section","location":"vnt/arraylikeblocks/#Getting-and-setting:-in-practice","page":"Array-like blocks","text":"As a user you should not have to deal with ArrayLikeBlocks directly. Under the hood, templated_setindex!! will automatically wrap values in ArrayLikeBlocks when necessary:\n\nx = zeros(5)\nvnt = templated_setindex!!(VarNamedTuple(), Dirichlet(ones(3)), @vn(x[1:3]), x)\n\nYou can access the value again as long as you refer to the full range:\n\nvnt[@vn(x[1:3])]\n\nBecause we provided template information, you can access this via any other combination of indexing, as long as it refers to all three indices:\n\nvnt[@vn(x[begin:(end-2)])]\n\nHowever, if you try to access only part of the block, you will get an error:\n\nvnt[@vn(x[1])]\n\nFurthermore, if you set a value into any of the indices covered by the block, the entire block is invalidated and thus removed:\n\nstruct Normal end # Again a fake struct to avoid importing Distributions.\n\nvnt = templated_setindex!!(vnt, Normal(), @vn(x[2]), x)","title":"Getting and setting: in practice"},{"category":"section","location":"vnt/manipulation/#Manipulation-of-VNTs","page":"Manipulation of VNTs","text":"Once you have created a VNT (or obtained it from elsewhere), there are a number of functions which can be used to modify its contents. These are faithfully documented in the API docs. Many of them are intended as iteration tools, such as mapreduce, and don't really require thorough explanation.\n\nHowever, here we will briefly discuss two of the more 'special' functions, which modify the representation of a VNT.","title":"Manipulation of VNTs"},{"category":"section","location":"vnt/manipulation/#Densification","page":"Manipulation of VNTs","text":"The function VarNames.densify!! converts VNTs to 'dense' representations: essentially, this means converting any PartialArrays whose masks are completely true to regular arrays.\n\nFor example, this VNT contains a PartialArray which is really the same thing as a regular array:\n\nusing VarNames\n\nvnt = @vnt begin\n    @template x = zeros(2)\n    x[1] := 1.0\n    x[2] := 2.0\nend\n\nCalling densify!! on it leads to:\n\ndense_vnt = densify!!(vnt)\n\nNote that vnt and dense_vnt contain the same values, and you can index into them in the same way:\n\nvnt[@vn(x)], dense_vnt[@vn(x)]\n\nHowever, they differ in the keys that they return.\n\nkeys(vnt), keys(dense_vnt)\n\nIn other words, vnt retains the notion that x[1] and x[2] are separate, whereas dense_vnt treats x as a single entity. In many cases the latter is more convenient.\n\nNote that densification, and the resulting improvements in ergonomics, is something that can only really be done with VNTs as a base data structure. If we had plain old OrderedDicts mapping x[1] => 1.0 and x[2] => 2.0, there would be no way to know that x only contained these two entries and nothing else. This goes back to our arguments about 'constructiveness' in the VNT motivation page.","title":"Densification"},{"category":"section","location":"vnt/manipulation/#Skeleton-VNTs","page":"Manipulation of VNTs","text":"Another function, VarNames.skeleton, is used to generate 'skeletons' of VNTs. Specifically, a skeleton of a VNT is one that contains enough template information to reconstruct the VNT from its key-value pairs.\n\nThis is best illustrated by example:\n\nvnt = @vnt begin\n    # We set x to be length-3 to avoid it ever being\n    # densified, which would render this example moot.\n    @template x = zeros(3)\n    x[1] := 1.0\n    x[2] := 2.0\n    # Stick in another scalar for illustration.\n    y := 3.0\nend\n\nSuppose we convert this into an OrderedDict. This is a lossy operation, since we lose the information that x is a length-3 vector.\n\nusing OrderedCollections\n\nod = OrderedDict(pairs(vnt)...)\n\nWe can't go back from this OrderedDict to the original VNT: if we attempt to naively do so, although y will be faithfully reconstructed, we will get a GrowableArray for x.\n\nnew_vnt = VarNamedTuple(od)\n\nClearly, what we are lacking is the template information for x. This is where the skeleton comes in:\n\nskel = skeleton(vnt)\n\nNotice that in the skeleton, the key y has been entirely dropped, since we don't need any template information for it. Furthermore, the PartialArray x has been replaced with a normal array with the same size and array type (i.e. Base.Array), but with all values set to nothing.\n\nHere, nothing is chosen because storing it in an array is extremely space-efficient:\n\nBase.sizeof(fill(nothing, 100))\n\nFurthermore, the element type of the array is not needed when reconstructing data. For example, this is how we would reconstruct the original VNT:\n\nusing VarNames: getsym\n\n# In a real setting you wouldn't need `begin .. end` and `local`.\nbegin\n    local new_vnt = VarNamedTuple()\n    for (vn, val) in pairs(od)\n        top_sym = getsym(vn)\n        template = get(skel.data, top_sym, VarNames.NoTemplate())\n        new_vnt = VarNames.templated_setindex!!(new_vnt, val, vn, template)\n    end\n    new_vnt\nend\n\nIn the call to templated_setindex!! for x, the template is fill(nothing, 3). However, the element type of this template array is not important: when VarNames instantiates the new PartialArray, it will use typeof(val) rather than eltype(template) to determine the element type it should use. This allows us to freely choose any element type we like in the skeleton.","title":"Skeleton VNTs"},{"category":"section","location":"vnt/api/#API","page":"API","text":"","title":"API"},{"category":"type","location":"vnt/api/#VarNames.VarNamedTuple","page":"API","text":"VarNamedTuple{names,Values}\n\nA NamedTuple-like structure with VarName keys.\n\nVarNamedTuple is a data structure for storing arbitrary data, keyed by VarNames, in an efficient and type stable manner. It is mainly used through getindex, setindex!!, templated_setindex!!, and haskey, all of which only accept VarNames as keys. Other notable methods are merge and subset.\n\nVarNamedTuple has an ordering to its elements, and two VarNamedTuples with the same keys and values but in different orders are considered different for equality and hashing. Iterations such as keys and values respect this ordering. The ordering is dependent on the order in which elements were inserted into the VarNamedTuple, though isn't always equal to it. More specifically\n\nAny new keys that have a joint parent VarName with an existing key are inserted after that key. For instance, if one first inserts, in order, @vn(a.x), @vn(b), and @vn(a.y), the resulting order will be (@vn(a.x), @vn(a.y), @vn(b)).\nIndex keys, like@vn(a[3])or@vn(b[2,3,4:5]), are always iterated in the same order anArraywith the same indices would be iterated. For instance, if one first inserts, in order,@vn(a[2]),@vn(b), and@vn(a[1]), the resulting order will be(@vn(a[1]), @vn(a[2]), @vn(b))`.\n\nOtherwise insertion order is respected.\n\nsetindex!! and getindex on VarNamedTuple are type stable as long as one does not store heterogeneous data under different indices of the same symbol. That is, if either\n\none sets a[1] and a[2] to be of different types, or\nif a[1] and a[2] both exist, one sets a[1].b without setting a[2].b,\n\nthen getting values for a[1] or a[2] will not be type stable.\n\n\n\n\n\n","title":"VarNames.VarNamedTuple"},{"category":"macro","location":"vnt/api/#VarNames.@vnt","page":"API","text":"@vnt begin ... end\n\nConstruct a VarNamedTuple from a block of assignments. Each assignment should be of the form var := value, where var is a variable name. This is best illustrated by example:\n\njulia> using VarNames\n\njulia> @vnt begin\n           a := 1\n           b := 2\n       end\nVarNamedTuple\n├─ a => 1\n└─ b => 2\n\nYou can set entirely arbitrary variables:\n\njulia> @vnt begin\n           a.b.c.d.e := \"hello\"\n       end\nVarNamedTuple\n└─ a => VarNamedTuple\n        └─ b => VarNamedTuple\n                └─ c => VarNamedTuple\n                        └─ d => VarNamedTuple\n                                └─ e => \"hello\"\n\nFor variables that have indexing, it is often necessary to provide a template, so that the VNT 'knows' what kind of array is being used to store the values, and can set the values in the appropriate places (consider e.g. OffsetArrays where x[1] may not mean what it usually does).\n\nThis is done by inserting a @template macro call in the block. The @template macro accepts whitespace-separated arguments, which must either be\n\na single symbol (e.g. @template x), in which case the template is the value of x (and x must already be defined in the current scope); or\nan assignment of the form y = expr, in which case the template for y is the value of expr. In this case y does not need to be defined in the current scope, but any symbols referenced in expr must be.\n\nFor example:\n\njulia> x = zeros(5); outside_y = zeros(3, 3);\n\njulia> @vnt begin\n            @template x y=outside_y\n            x[1] := 1.0\n            y[1, 1] := 2.0\n       end\nVarNamedTuple\n├─ x => PartialArray size=(5,) data::Vector{Float64}\n│       └─ (1,) => 1.0\n└─ y => PartialArray size=(3, 3) data::Matrix{Float64}\n        └─ (1, 1) => 2.0\n\nnote: Note\nYou can use any expression in @template y=expr, even a function call that is completely contained within the macro (e.g. @template y=zeros(3, 3)). The macro makes sure that expr is only evaluated once, so there is no performance penalty to doing this.\n\nIf no template is provided, the VNT will use a GrowableArray. This can produce correct results in simple cases, but is not recommended for general use. Please see the VarNamedTuple documentation for more details.\n\n\n\n\n\n","title":"VarNames.@vnt"},{"category":"function","location":"vnt/api/#BangBang.empty!!","page":"API","text":"empty!!(vnt::VarNamedTuple)\n\nCreate an empty version of vnt in place.\n\nThis differs from Base.empty in that any PartialArrays contained within vnt are kept but have their contents deleted, rather than being removed entirely. This means that\n\nThe result has a \"memory\" of how many dimensions different variables had, and you cannot, for example, set a[1,2] after emptying a VarNamedTuple that had only a[1] defined.\nMemory allocations may be reduced when reusing VarNamedTuples, since the internal PartialArrays do not need to be reallocated from scratch.\n\n\n\n\n\n","title":"BangBang.empty!!"},{"category":"function","location":"vnt/api/#BangBang.setindex!!","page":"API","text":"BangBang.setindex!!(vnt::VarNamedTuple, value, vn::VarName)\n\nThis is similar to templated_setindex!!, but does not take a template argument. In effect the template passed through is NoTemplate(). It is often the case that the template is not actually needed (for example, if you are setting a top-level VarName: @vn(a), or if the VarName only contains Property optics, or if the arrays already exist and you are merely updating a value inside it). In such cases, this method will work fine, but may throw an error if the template is actually needed. Specifically, this is likely to happen if you set a VarName with dynamic optics or colons, since in those cases the size and shape of the underlying arrays cannot be inferred without a template.\n\nFor example, this will error, since it is not known what x should be:\n\nvnt = VarNamedTuple()\nsetindex!!(vnt, 10, @vn(x[1]))\n\n\n\n\n\n","title":"BangBang.setindex!!"},{"category":"function","location":"vnt/api/#VarNames.apply!!","page":"API","text":"apply!!(func, vnt::VarNamedTuple, name::VarName)\n\nApply func to the subdata at name in vnt, and set the result back at name.\n\nLike map_values!!, but only for a single VarName.\n\njulia> using VarNames: VarNamedTuple, setindex!!, apply!!\n\njulia> vnt = VarNamedTuple()\nVarNamedTuple()\n\njulia> vnt = setindex!!(vnt, [1, 2, 3], @vn(a))\nVarNamedTuple\n└─ a => [1, 2, 3]\n\njulia> apply!!(x -> x .+ 1, vnt, @vn(a))\nVarNamedTuple\n└─ a => [2, 3, 4]\n\n\n\n\n\n","title":"VarNames.apply!!"},{"category":"function","location":"vnt/api/#VarNames.map_pairs!!","page":"API","text":"map_pairs!!(func, vnt::VarNamedTuple)\n\nApply func to all key => value pairs of vnt, in place if possible.\n\nfunc should accept a pair of VarName and value, and return the new value to be set.\n\n\n\n\n\n","title":"VarNames.map_pairs!!"},{"category":"function","location":"vnt/api/#VarNames.map_values!!","page":"API","text":"map_values!!(func, vnt::VarNamedTuple)\n\nApply func to elements of vnt, in place if possible.\n\n\n\n\n\n","title":"VarNames.map_values!!"},{"category":"function","location":"vnt/api/#VarNames.subset","page":"API","text":"subset(vnt::VarNamedTuple, vns)\n\nCreate a new VarNamedTuple containing only the variables subsumed by ones in vns.\n\n\n\n\n\n","title":"VarNames.subset"},{"category":"type","location":"vnt/api/#VarNames.NoTemplate","page":"API","text":"NoTemplate\n\nA singleton struct representing the fact that there is no template for a top-level variable. When NoTemplate is used, several things happen:\n\nIf you attempt to call make_leaf with an Index optic, this will error.\nWhen recursing into substructures, NoTemplate will be propagated.\n\nCollectively this means that you can only set values for variables that only have Property optics, unless a template is provided.\n\nIt might seem more idiomatic to use nothing or missing for this. However, this causes a bug with BangBang.setindex!!: https://github.com/JuliaFolds2/BangBang.jl/issues/43 so we use a dedicated struct instead.\n\n\n\n\n\n","title":"VarNames.NoTemplate"},{"category":"type","location":"vnt/api/#VarNames.SkipTemplate","page":"API","text":"SkipTemplate{N}(value)\n\nA struct representing the fact that value is the template for the variable N levels down from the top-level variable. In other words, SkipTemplate{0}(value) is equivalent to just value, and SkipTemplate{1}(value) means that value is the template for a when setting the variable @vn(x.a).\n\n\n\n\n\n","title":"VarNames.SkipTemplate"},{"category":"function","location":"vnt/api/#VarNames.templated_setindex_no_overwrite!!","page":"API","text":"templated_setindex_no_overwrite!!(vnt, value, vn, template)\n\nThis is the same as templated_setindex!!, but throws an error if the location specified by vn already has a value in vnt. This is useful for ensuring that values in a VarNamedTuple are not set multiple times.\n\n\n\n\n\n","title":"VarNames.templated_setindex_no_overwrite!!"},{"category":"function","location":"vnt/api/#VarNames.templated_setindex!!","page":"API","text":"templated_setindex!!(vnt, value, vn, template; allow_new=Val(true))\n\nAssign value to the location in vnt specified by vn.\n\nThe argument template must be provided in order to guide the creation of PartialArrays, as well as to concretise any dynamic indices in vn. It must be an object that has the shape of the top-level symbol in vn. For example:\n\nvnt = VarNamedTuple()\ntemplated_setindex!!(vnt, 10, @vn(x[1]), rand(2, 2))\n\nHere, rand(2, 2) is the template for the top-level symbol x, which tells setindex!! that x should be a PartialArray that is backed by a matrix.\n\nThe actual data inside template is not needed, and template is never mutated by this call.\n\n\n\n\n\n","title":"VarNames.templated_setindex!!"},{"category":"function","location":"vnt/api/#VarNames.densify!!","page":"API","text":"densify!!(vnt::VarNamedTuple)\n\nConvert any PartialArrays in vnt where all elements are filled to just normal arrays.\n\n\n\n\n\n","title":"VarNames.densify!!"},{"category":"function","location":"vnt/api/#VarNames.skeleton","page":"API","text":"skeleton(vnt::VarNamedTuple)\n\nCreate a skeleton of the given VarNamedTuple, which holds enough template information to reconstruct the original vnt from an ordered set of (VarName, value) pairs.\n\nSpecifically, we require that for any input orig_vnt that the following holds true:\n\nskel = skeleton(orig_vnt)\n# Check roundtrip reconstruction\nnew_vnt = VarNamedTuple()\nfor (vn, val) in pairs(orig_vnt)\n    top_sym = getsym(vn)\n    template = get(skel.data, top_sym, NoTemplate())\n    new_vnt = templated_setindex!!(new_vnt, val, vn, template)\nend\n@assert new_vnt == orig_vnt\n\nThis is accomplished by recursively traversing vnt and dropping any elements that do not contain PartialArrays. PartialArrays on the other hand are replaced with arrays of the same shape and array type, but with the elements replaced with nothing (since that is space-efficient). This does not cause any issues with reconstruction type stability, since templated_setindex!! does not use the array's element type when creating a new entry (it infers the element type to use from the value that is being set).\n\n\n\n\n\n","title":"VarNames.skeleton"},{"category":"section","location":"vnt/design/#Design","page":"Design","text":"There are two aspects to the design of VarNamedTuples: property access, and indexing. VarNamedTuple consists of recursively nested NamedTuples and PartialArrays which together support both kinds of access.","title":"Design"},{"category":"section","location":"vnt/design/#Property-access","page":"Design","text":"Let's first talk about the NamedTuple part. In a VarNamedTuple each level of a Property optic corresponds to a level of nested NamedTuples, with the Symbols of the lenses as keys. For instance, the VarNamedTuple mapping @vn(x) => 1, @vn(y.z) => 2 would be stored as\n\nVarNamedTuple(; x=1, y=VarNamedTuple(; z=2))\n\nwhere VarNamedTuple(; x=a, y=b) is just a thin wrapper around the NamedTuple (; x=a, y=b). In fact, if vnt is a VarNamedTuple, then vnt.data is exactly that underlying NamedTuple.\n\nIt's often handy to think of this as a tree, with each node being a VarNamedTuple, like so:\n\n   VNT\nx /   \\ y\n 1     VNT\n         \\ z\n          2\n\nBy virtue of these being NamedTuple, all variable access is completely type-stable.\n\nIf all VarNames consisted of only Propertyes we would be done designing the data structure. Sadly, that isn't the case!","title":"Property access"},{"category":"section","location":"vnt/design/#Indexing-and-PartialArrays","page":"Design","text":"Indexing is much more complicated to handle than property access, for a number of reasons.\n\nLet's start by talking about the most obvious issue: only some parts of an array may be set. For example, the user may set x[1] and x[10] but not the ones in the middle. This is accomplished using a PartialArray, which is a wrapper around an AbstractArray that holds the data of interest, together with a mask saying which elements have been set.\n\nThe array type and size of the mask is always the same as the data array (this is done using Base.similar).\n\ninfo: Info\nCurrently this is not enforced in an inner constructor, because that sometimes leads to extra allocations. It would be nice to investigate if this invariant can be enforced.\n\nIf pa.mask[i] is true, then pa.data[i] has been set; otherwise, pa.data[i] may contain some value, but it is not valid to index into that part of the array.\n\nHere is an example:\n\nusing VarNames: PartialArray\n\ndata = randn(3)\nmask = similar(data, Bool)\nfill!(mask, false)\npa = PartialArray(data, mask)\n\nThe main way of interacting with a PartialArray is to use BangBang.setindex!! (which VarNames reexports). This sets one or more elements of the PartialArray's data, and marks the corresponding elements in the mask as true.\n\nusing VarNames: setindex!!\n\nsetindex!!(pa, 12.0, 2)\npa.data, pa.mask\n\nWhen printed, pa shows only the elements that are set:\n\npa\n\nIt is invalid to index into the unset elements:\n\npa[1]\n\nbut you can access the set elements:\n\npa[2]\n\nIt follows from this, that because the PartialArray is actually backed by a regular Array, it is constructive. The call to getindex will return the values stored in pa.data, as long as all the elements in pa.mask are set. For example:\n\nsetindex!!(pa, 11.0, 1)\npa[1:2]\n\nYou can even get the entirety of the array, once you have set all three elements. In this case, the PartialArray is (morally) equivalent to a single Array.\n\nsetindex!!(pa, 13.0, 3)\npa[:]\n\nAll Index optics in VarNames correspond to PartialArrays in VarNamedTuples. For example, let's say we want to store the mappings @vn(x[1].a) => 1.0, and y.b[2,3] => 2.0. The corresponding VarNamedTuple would look like this:\n\n         VNT\n        /   \\y  \n      x/     \\    b\n      /     VNT------PA[[ _, _, _  ],\n     /                  [ _, _, 2.0]]\n    PA [VNT]\n         |\n        a|\n         |\n        1.0\n\nwhere _ indicates masked elements in a PartialArray. To demonstrate:\n\nusing VarNames\n\nvnt = VarNamedTuple()\nvnt = setindex!!(vnt, 1.0, @vn(x[1].a))\nvnt = setindex!!(vnt, 2.0, @vn(y.b[2, 3]))\n\nvnt.data.x  # This is a PartialArray\n\nvnt.data.x.data[1]   # This is a VNT\n\nvnt.data.x.data[1].data.a  # This is 1.0\n\nvnt.data.y  # This is a VNT\n\nvnt.data.y.data.b  # This is a PartialArray\n\nvnt.data.y.data.b.data[2, 3]  # This is 2.0\n\nThis illustrates the fundamental structure of VarNamedTuples. From this example, one can see that getting data from a VarNamedTuple is as type-stable as possible:\n\nAll property accesses are NamedTuple accesses, which are type-stable.\nAll indexing is done into PartialArrays: as long as indexing into the underlying data is type-stable (i.e., the element type of the data array is concrete), indexing into the PartialArray is type-stable as well.\n\ninfo: Info\nIn fact, the underlying data need not all have the same (concrete) type: all that is needed is that the unmasked elements have the same (concrete) type. The function _concretise_eltype!! is an attempt to force this to be the case: if the element type is abstract, but all the set elements have the same concrete type, the entire data array's element type will be changed to that concrete type (with junk in the unset elements).\n\nOne immediate question here is: how do we know what kind of array the PartialArray should use for its data and mask?","title":"Indexing and PartialArrays"},{"category":"section","location":"vnt/design/#GrowableArrays","page":"Design","text":"It's not obvious in the code above, but in the example above, we are implicitly making an assumption based on the indices that we see in the VarNames. For example, for @vn(x[1].a), based on the index 1 we assume that x should be a vector with a length of at least 1. Similarly, for @vn(y.b[2,3]), we assume that y.b should be a matrix with at least 2 rows and 3 columns.\n\nWe can inspect this by looking into the PartialArrays:\n\nvnt.data.x.data\n\nvnt.data.y.data.b.data\n\nSo, these PartialArrays are backed by something called GrowableArray. A GrowableArray is an array type, defined in VarNames, that can grow in size as needed when setindex!! is called with indices outside of its current bounds (with other arrays that would error). The reason for such an array type is that you may want to do something like\n\nbegin\n    local vnt = VarNamedTuple()\n    for i in 1:5\n        vnt = setindex!!(vnt, i, @vn(x[i]))\n    end\n    vnt\nend\n\nand we don't have the ability to know in advance that x will eventually need to be at least of size 5. So, every call to setindex!! here will cause the underlying GrowableArray to grow in size as needed.\n\nThe problem with this is that it makes a huge number of implicit assumptions about what kind of array x actually is, and consequently it forbids a huge number of indexing operations in Julia.\n\nThese include, for example, linear indexing. In this example, the first call will create a GrowableArray with one dimension (i.e., a vector); and the second call will fail since we can't index a vector with two indices.\n\nvnt = setindex!!(VarNamedTuple(), 10.0, @vn(x[1]))\nvnt = setindex!!(vnt, 20.0, @vn(x[2, 2]))\n\nColons also don't work.\n\nvnt = setindex!!(VarNamedTuple(), randn(2), @vn(x[:]))\n\nOther things like OffsetArrays don't work (because x[11] would create a length-11 GrowableArray, which might not be appropriate; and x[-1] will just straight-up error). DimArrays will also fail if you use an index that isn't an integer.\n\nFinally, we don't know how large the final size of the array should be. If you try to access the entire array after setting some elements, it will work, but it will warn you:\n\nvnt = setindex!!(VarNamedTuple(), 10.0, @vn(x[1]))\nvnt[@vn(x)]","title":"GrowableArrays"},{"category":"section","location":"vnt/design/#Templated-arrays","page":"Design","text":"The general solution to this problem is for the user to provide a template for the array x in advance, so that we know what kind of array to create when we see @vn(x[...]).\n\nAt a low level, this is done using the templated_setindex!! function, which takes an extra argument that specifies the shape of the top-level symbol in the VarName.\n\nFor example, the linear-indexing example above now works if you tell the function that x is a 2-by-2 matrix.\n\nusing VarNames: templated_setindex!!\n\nx = zeros(2, 2)\nvnt = VarNamedTuple()\nvnt = templated_setindex!!(VarNamedTuple(), 10.0, @vn(x[1]), x)\nvnt = setindex!!(vnt, 20.0, @vn(x[2, 2]))\n\n(The second call can also be templated_setindex!!, but it isn't necessary since the first call already establishes the shape of x, and indeed in such a case the template will be ignored.) Notice that the PartialArray is now backed by the correct Array:\n\nvnt.data.x.data\n\nIt is no longer growable, so if you try to set an out-of-bounds index, it will error:\n\nvnt = setindex!!(vnt, 30.0, @vn(x[3, 1]))\n\nThis mechanism makes it far more flexible to work with arrays. Fundamentally, this resolves the inconsistency between indexing semantics in the actual varaible x, and indexing semantics of the x inside the VarNamedTuple.\n\nFor example, you can use DimArrays:\n\nimport DimensionalData as DD\n\nx = DD.DimArray(zeros(2, 3), (DD.X, DD.Y))\n\nvnt = VarNamedTuple()\nvnt = templated_setindex!!(vnt, 1.0, @vn(x[DD.X(1), DD.Y(2)]), x)\n\nvnt.data.x.data\n\nYou can access the data back again in any way you like, for example using linear indexing here:\n\ngetindex(vnt, @vn(x[3]))\n\ninfo: Info\nNote that support for such arrays is contingent on the provider of the array type, as well as BangBang.jl. There may be bugs that prevent some array types from fully working correctly. For example, BangBang.setindex!! does not accept keyword arguments, which precludes the use of keyword indices in DimArrays. However, VarNames itself does not inherently prevent you from using such arrays. We would definitely like to fix upstream issues like these, but we don't always have the time to do so: help is very greatly appreciated!","title":"Templated arrays"},{"category":"section","location":"varname/#varname","page":"VarName","text":"The VarName type represents names of random variables.\n\nFundamentally, a VarName comprises a symbol (which represents the name of the variable itself) and an optic (which tells us which part of the variable we might be interested in). For example, x.a[1] means the first element of the field a of the variable x. Here, x is the symbol, and .a[1] is the optic.\n\nVarNames can be created using the @vn macro:\n\nusing VarNames\n\nvn = @vn(x.a[1])\n\nYou can obtain the components of a VarName using the getsym and getoptic functions:\n\ngetsym(vn), getoptic(vn)","title":"VarName"},{"category":"section","location":"varname/#Dynamic-indices","page":"VarName","text":"VarNames may contain 'dynamic' indices, that is, indices whose meaning is not known until they are resolved against a specific value. For example, x[end] refers to the last element of x; but we don't know what that means until we know what x is.\n\nSpecifically, begin and end symbols in indices are treated as dynamic indices. This is also true for any expression that contains begin or end, such as end-1 or 1:3:end.\n\nDynamic indices are represented using an internal type, VarNames.DynamicIndex.\n\nvn_dyn = @vn(x[1:2:end])\n\nYou can detect whether a VarName contains dynamic indices using the is_dynamic function:\n\nis_dynamic(vn_dyn)\n\nThese dynamic indices can be resolved, or concretized, by passing a specific value to the concretize function:\n\nx = randn(5)\nvn_conc = concretize(vn_dyn, x)","title":"Dynamic indices"},{"category":"section","location":"varname/#Optics","page":"VarName","text":"The optics used in VarNames.jl are represented as a linked list. For example, the optic .a[1] is a Property optic that contains an Index optic as its child. That means that the 'elements' of the linked list can be read from left-to-right:\n\nProperty{:a} -> Index{1} -> Iden\n\nAll optic linked lists are terminated with an Iden optic, which represents the identity function.\n\noptic = getoptic(@vn x.a[1])\ndump(optic)\n\nInstead of calling getoptic(@vn(...)), you can directly use the @opticof macro to create optics:\n\noptic = @opticof(_.a[1])","title":"Optics"},{"category":"section","location":"varname/#Getting-and-setting","page":"VarName","text":"Optics are callable structs, and when passed a value will extract the relevant part of that value.\n\ndata = (a=[10, 20, 30], b=\"hello\")\noptic = @opticof(_.a[2])\noptic(data)\n\nYou can set values using Accessors.set (which VarNames re-exports). Note, though, that this will not mutate the original value. Furthermore, you cannot use the handy macros like Accessors.@set, since those will use the optics from Accessors.jl.\n\nnew_data = set(data, optic, 99)\nnew_data, data\n\nIf you want to try to mutate values, you can wrap an optic using with_mutation.\n\noptic_mut = with_mutation(optic)\nset(data, optic_mut, 99)\ndata","title":"Getting and setting"},{"category":"section","location":"varname/#Composing-and-decomposing-optics","page":"VarName","text":"If you have two optics, you can compose them using the ∘ operator:\n\noptic1 = @opticof(_.a)\noptic2 = @opticof(_[1])\ncomposed = optic2 ∘ optic1\n\nNotice the order of composition here, which can be counterintuitive: optic2 ∘ optic1 means \"first apply optic1, then apply optic2\", and thus this represents the optic .a[1] (not .[1].a).\n\nBase.cat(optics...) is also provided, which composes optics in a more intuitive sense (indeed, if you think of an optic as a linked list, this can be thought of as concatenating the lists). The following is equivalent to the previous example:\n\ncomposed2 = Base.cat(optic1, optic2)\n\nappend_optic is a shortcut for 'composing' an optic with a VarName.\n\nSeveral functions are provided to decompose optics, which all stem from their linked-list structure. Their names directly mirror Haskell's functions for decomposing lists, but are prefixed with o:\n\nFor example, ohead returns the first element of the optic linked list, and otail returns the rest of the list after removing the head:\n\noptic = @opticof(_.a[1].b[2])\nohead(optic), otail(optic)\n\nConvesely, oinit returns the optic linked list without its last element, and olast returns the last element:\n\noinit(optic), olast(optic)\n\nIf the optic only has a single element, then oinit and otail return Iden, while ohead and olast return the optic itself:\n\noptic_single = @opticof(_.a)\noinit(optic_single), olast(optic_single), ohead(optic_single), otail(optic_single)","title":"Composing and decomposing optics"},{"category":"section","location":"varname/#Converting-VarNames-to-optics-and-back","page":"VarName","text":"Sometimes it is useful to treat a VarName's top level symbol as if it were part of the optic. For example, when indexing into a NamedTuple nt, we might want to treat the entire VarName x.a[1] as an optic that can be applied to a NamedTuple: i.e., we want to access the nt.x field rather than the variable x itself. This can be achieved with:","title":"Converting VarNames to optics and back"},{"category":"section","location":"varname/#Subsumption","page":"VarName","text":"Sometimes, we want to check whether one VarName 'subsumes' another; that is, whether a VarName refers to a part of another VarName. This is done using the subsumes function:\n\nvn1 = @vn(x.a)\nvn2 = @vn(x.a[1])\nsubsumes(vn1, vn2)","title":"Subsumption"},{"category":"section","location":"varname/#Prefixing-and-unprefixing","page":"VarName","text":"Composing two optics can be done using the ∘ operator, as shown above. But what if we want to compose two VarNames?","title":"Prefixing and unprefixing"},{"category":"section","location":"varname/#VarName-leaves","page":"VarName","text":"The following functions are used to extract the 'leaves' of a VarName, that is, the atomic components of a VarName that do not have any further substructure. For example, for a vector variable x, the leaves would be x[1], x[2], etc.","title":"VarName leaves"},{"category":"section","location":"varname/#Reading-from-a-container-with-a-VarName-(or-optic)","page":"VarName","text":"","title":"Reading from a container with a VarName (or optic)"},{"category":"section","location":"varname/#Serializing-VarNames","page":"VarName","text":"","title":"Serializing VarNames"},{"category":"type","location":"varname/#VarNames.VarName","page":"VarName","text":"VarName{sym}(optic=identity)\n\nA variable identifier for a symbol sym and optic optic. sym refers to the name of the  top-level Julia variable, while optic allows one to specify a particular property or index inside that variable.\n\nVarNames can be manually constructed using the VarName{sym}(optic) constructor, or from an optic expression through the @vn convenience macro.\n\n\n\n\n\n","title":"VarNames.VarName"},{"category":"macro","location":"varname/#VarNames.@vn","page":"VarName","text":"@vn(expr, concretize=false)\n\nCreate a VarName given an expression expr representing a variable or part of it.\n\nBasic examples\n\nIn general, VarNames must have a top-level symbol representing the identifier itself, and can then have any number of property accesses or indexing operations chained to it.\n\njulia> @vn(x)\nx\n\njulia> @vn(x.a.b.c)\nx.a.b.c\n\njulia> @vn(x[1][2][3])\nx[1][2][3]\n\njulia> @vn(x.a[1:3].b[2])\nx.a[1:3].b[2]\n\nDynamic indices\n\nSome expressions may involve dynamic indices, specifically, begin, end. These indices cannot be resolved, or 'concretized', until the value being indexed into is known. By default, @vn(...) will not automatically concretize these expressions, and thus the resulting VarName will contain markers for these.\n\nNote that colons are not considered dynamic.\n\njulia> vn = @vn(x[end])\nx[DynamicIndex(end)]\n\njulia> vn = @vn(x[1, end-1])\nx[1, DynamicIndex(end - 1)]\n\nYou can detect whether a VarName contains any dynamic indices using is_dynamic:\n\njulia> vn = @vn(x[1, end-1]); is_dynamic(vn)\ntrue\n\nTo concretize such expressions, you can call concretize on the resulting VarName. After concretization, the resulting VarName will no longer be dynamic.\n\njulia> x = randn(2, 3);\n\njulia> vn = @vn(x[1, end-1]); vn2 = concretize(vn, x)\nx[1, 2]\n\njulia> getoptic(vn2).ix  # Just an ordinary tuple.\n(1, 2)\n\njulia> is_dynamic(vn2)\nfalse\n\nAlternatively, you can pass true as the second positional argument to the @vn macro (note that it is not a keyword argument!). This will automatically call concretize for you, using the top-level symbol to look up the value used for concretization.\n\njulia> x = randn(2, 3);\n\njulia> @vn(x[1:end, end][:], true)\nx[1:2, 3][:]\n\nInterpolation\n\nProperty names, as well as top-level symbols, can also be constructed from interpolated symbols:\n\njulia> name = :hello; @vn(x.$name)\nx.hello\n\njulia> @vn($name)\nhello\n\njulia> @vn($name.a.$name[1])\nhello.a.hello[1]\n\nFor indices, you do not need to use $ to interpolate, just use the variable directly:\n\njulia> ix = 2; @vn(x[ix])\nx[2]\n\nNote that if the top-level symbol is interpolated, automatic concretization is not possible:\n\njulia> name = :x; @vn($name[1:end], true)\nERROR: LoadError: cannot automatically concretize VarName with interpolated top-level symbol; call `concretize(vn, val)` manually instead\n[...]\n\n\n\n\n\n","title":"VarNames.@vn"},{"category":"function","location":"varname/#VarNames.varname","page":"VarName","text":"varname(expr, should_concretize::Bool)\n\nImplementation of the @vn macro. See the documentation for @vn for details. This function is exported to allow other macros to reuse the same logic.\n\n\n\n\n\n","title":"VarNames.varname"},{"category":"function","location":"varname/#VarNames.getsym","page":"VarName","text":"getsym(vn::VarName)\n\nReturn the symbol of the Julia variable used to generate vn.\n\nExamples\n\njulia> getsym(@vn(x[1][2:3]))\n:x\n\njulia> getsym(@vn(y))\n:y\n\n\n\n\n\n","title":"VarNames.getsym"},{"category":"function","location":"varname/#VarNames.getoptic","page":"VarName","text":"getoptic(vn::VarName)\n\nReturn the optic of the Julia variable used to generate vn.\n\nExamples\n\njulia> getoptic(@vn(x[1][2:3]))\nOptic([1][2:3])\n\njulia> getoptic(@vn(y))\nOptic()\n\n\n\n\n\n","title":"VarNames.getoptic"},{"category":"function","location":"varname/#VarNames.is_dynamic","page":"VarName","text":"is_dynamic(vn::VarName)\n\nReturn true if vn contains any dynamic indices (i.e., begin and end). If a VarName has been concretized, this will always return false.\n\n\n\n\n\n","title":"VarNames.is_dynamic"},{"category":"function","location":"varname/#VarNames.concretize","page":"VarName","text":"concretize(idx::Index, val)\n\nRecursively concretize all dynamic indices in idx (including its child optics) against val.\n\n\n\n\n\nconcretize(vn::VarName, x)\n\nReturn vn concretized on x, i.e. any information related to the runtime shape of x is evaluated. This will convert any begin and end indices in vn to concrete indices with information about the length of the dimension being indexed into.\n\n\n\n\n\n","title":"VarNames.concretize"},{"category":"function","location":"varname/#VarNames.concretize_top_level","page":"VarName","text":"concretize_top_level(idx::Index, val)\n\nConcretise only the indices of idx against val, leaving the child optic unconcretised.\n\n\n\n\n\n","title":"VarNames.concretize_top_level"},{"category":"type","location":"varname/#VarNames.AbstractOptic","page":"VarName","text":"AbstractOptic\n\nAn abstract type that represents the non-symbol part of a VarName, i.e., the section of the variable that is of interest. For example, in x.a[1][2], the AbstractOptic represents the .a[1][2] part.\n\n\n\n\n\n","title":"VarNames.AbstractOptic"},{"category":"type","location":"varname/#VarNames.Property","page":"VarName","text":"Property{sym}(child=Iden())\n\nA property access optic representing access to property sym. A VarName{:x} with this optic represents access to x.sym. The child optic represents any further indexing or property access after this property access operation.\n\n\n\n\n\n","title":"VarNames.Property"},{"category":"type","location":"varname/#VarNames.Index","page":"VarName","text":"Index(ix, kw, child=Iden())\n\nAn indexing optic representing access to indices ix, which may also take the form of keyword arguments kw. A VarName{:x} with this optic represents access to x[ix..., kw...]. The child optic represents any further indexing or property access after this indexing operation.\n\n\n\n\n\n","title":"VarNames.Index"},{"category":"type","location":"varname/#VarNames.Iden","page":"VarName","text":"Iden()\n\nThe identity optic. This is the optic used when we are referring to the entire variable. It is also the base case for composing optics.\n\n\n\n\n\n","title":"VarNames.Iden"},{"category":"macro","location":"varname/#VarNames.@opticof","page":"VarName","text":"@opticof(expr, concretize=false)\n\nExtract the optic from @vn(expr, concretize). This is a thin wrapper around getoptic(@vn(...)).\n\nIf you don't need to concretize, you should use _ as the top-level symbol to indicate that it is not relevant:\n\njulia> @opticof(_.a.b)\nOptic(.a.b)\n\nIf you need to concretize, then you can provide a real variable name (which is then used to look up the value for concretization):\n\njulia> x = randn(3, 4); @opticof(x[1:end, end], true)\nOptic([1:3, 4])\n\nNote that concretization with @opticof has the same limitations as with @vn, specifically, if the top-level symbol is interpolated, automatic concretization is not possible.\n\n\n\n\n\n","title":"VarNames.@opticof"},{"category":"function","location":"varname/#VarNames.with_mutation","page":"VarName","text":"with_mutation(o::AbstractOptic)\n\nCreate a version of the optic o which attempts to mutate its input where possible.\n\nOn their own, AbstractOptics are non-mutating:\n\njulia> optic = @opticof(_[1])\nOptic([1])\n\njulia> x = [0.0, 0.0];\n\njulia> set(x, optic, 1.0); x\n2-element Vector{Float64}:\n 0.0\n 0.0\n\nWith this function, we can create a mutating version of the optic:\n\njulia> optic_mut = with_mutation(@opticof(_[1]))\nOptic!!([1])\n\njulia> x = [0.0, 0.0];\n\njulia> set(x, optic_mut, 1.0); x\n2-element Vector{Float64}:\n 1.0\n 0.0\n\nThanks to the BangBang.jl package, this optic will gracefully fall back to non-mutating behaviour if mutation is not possible. For example, if we try to use it on a tuple:\n\njulia> optic_mut = with_mutation(@opticof(_[1]))\nOptic!!([1])\n\njulia> x = (0.0, 0.0);\n\njulia> set(x, optic_mut, 1.0); x\n(0.0, 0.0)\n\n\n\n\n\n","title":"VarNames.with_mutation"},{"category":"method","location":"varname/#Base.:∘-Tuple{AbstractOptic, AbstractOptic}","page":"VarName","text":"∘(outer::AbstractOptic, inner::AbstractOptic)\n\nCompose two AbstractOptics together.\n\njulia> p1 = @opticof(_.a[1])\nOptic(.a[1])\n\njulia> p2 = @opticof(_.b[2, 3])\nOptic(.b[2, 3])\n\njulia> p1 ∘ p2\nOptic(.b[2, 3].a[1])\n\n\n\n\n\n","title":"Base.:∘"},{"category":"method","location":"varname/#Base.cat-Tuple{Vararg{AbstractOptic}}","page":"VarName","text":"cat(optics::AbstractOptic...)\n\nCompose multiple AbstractOptics together. The optics should be provided from innermost to outermost, i.e., cat(o1, o2, o3) corresponds to o3 ∘ o2 ∘ o1.\n\n\n\n\n\n","title":"Base.cat"},{"category":"function","location":"varname/#VarNames.append_optic","page":"VarName","text":"append_optic(vn::VarName, optic::AbstractOptic)\n\nCompose optic with the optic in vn, returning a new VarName.\n\noptic is placed at the tail of the existing optic, e.g.\n\njulia> vn = @vn(x.a.b)\nx.a.b\n\njulia> append_optic(vn, @opticof(_[1]))\nx.a.b[1]\n\n\n\n\n\n","title":"VarNames.append_optic"},{"category":"function","location":"varname/#VarNames.ohead","page":"VarName","text":"ohead(optic::AbstractOptic)\n\nGet the innermost layer of an optic. For all optics, we have that otail(optic) ∘ ohead(optic) == optic.\n\njulia> ohead(@opticof _.a[1][2])\nOptic(.a)\n\njulia> ohead(@opticof _)\nOptic()\n\n\n\n\n\n","title":"VarNames.ohead"},{"category":"function","location":"varname/#VarNames.otail","page":"VarName","text":"otail(optic::AbstractOptic)\n\nGet everything but the innermost layer of an optic. For all optics, we have that otail(optic) ∘ ohead(optic) == optic.\n\njulia> otail(@opticof _.a[1][2])\nOptic([1][2])\n\njulia> otail(@opticof _)\nOptic()\n\n\n\n\n\n","title":"VarNames.otail"},{"category":"function","location":"varname/#VarNames.oinit","page":"VarName","text":"oinit(optic::AbstractOptic)\n\nGet everything but the outermost layer of an optic. For all optics, we have that olast(optic) ∘ oinit(optic) == optic.\n\njulia> oinit(@opticof _.a[1][2])\nOptic(.a[1])\n\njulia> oinit(@opticof _)\nOptic()\n\n\n\n\n\n","title":"VarNames.oinit"},{"category":"function","location":"varname/#VarNames.olast","page":"VarName","text":"olast(optic::AbstractOptic)\n\nGet the outermost layer of an optic. For all optics, we have that olast(optic) ∘ oinit(optic) == optic.\n\njulia> olast(@opticof _.a[1][2])\nOptic([2])\n\njulia> olast(@opticof _)\nOptic()\n\n\n\n\n\n","title":"VarNames.olast"},{"category":"function","location":"varname/#VarNames.varname_to_optic","page":"VarName","text":"varname_to_optic(vn::VarName)\n\nConvert a VarName to an optic, by converting the top-level symbol to a Property optic.\n\n\n\n\n\n","title":"VarNames.varname_to_optic"},{"category":"function","location":"varname/#VarNames.optic_to_varname","page":"VarName","text":"optic_to_varname(optic::Property{sym}) where {sym}\n\nConvert a Property optic to a VarName, by converting the top-level property to a symbol. This fails for all other optics.\n\n\n\n\n\n","title":"VarNames.optic_to_varname"},{"category":"function","location":"varname/#VarNames.subsumes","page":"VarName","text":"subsumes(parent::VarName, child::VarName)\n\nCheck whether the variable name child describes a sub-range of the variable parent, i.e., is contained within it.\n\njulia> subsumes(@vn(x), @vn(x[1, 2]))\ntrue\n\njulia> subsumes(@vn(x[1, 2]), @vn(x[1, 2][3]))\ntrue\n\nThis is done by recursively comparing each layer of the VarNames' optics.\n\nNote that often this is not possible to determine statically, and so the results should not be over-interpreted. In particular, Index optics  pose a problem. An i::Index will only subsume j::Index if:\n\nThey have the same number of positional indices (i.ix and j.ix);\nEach positional index in i can be determined to comprise the corresponding positional index in j; and\nThe keyword indices of i (i.kw) are a superset of those in j.kw).\n\nIn all other cases, subsumes will conservatively return false, even though in practice it might well be that i does subsume j. Some examples where subsumption cannot be determined statically are:\n\nSubsumption between different forms of indexing is not supported, e.g. x[4] and x[2, 2] are not considered to subsume each other, even though they might in practice (e.g. if x is a 2x2 matrix).\nWhen dynamic indices (that are not equal) are present. (Dynamic indices that are equal do subsume each other.)\nNon-standard indices, e.g. Not(4), 2..3, etc. Again, these only subsume each other when they are equal.\n\n\n\n\n\n","title":"VarNames.subsumes"},{"category":"function","location":"varname/#VarNames.prefix","page":"VarName","text":"prefix(vn::VarName, prefix::VarName)\n\nAdd a prefix to a VarName.\n\njulia> prefix(@vn(x), @vn(y))\ny.x\n\njulia> prefix(@vn(x.a), @vn(y))\ny.x.a\n\njulia> prefix(@vn(x.a), @vn(y[1]))\ny[1].x.a\n\n\n\n\n\n","title":"VarNames.prefix"},{"category":"function","location":"varname/#VarNames.unprefix","page":"VarName","text":"unprefix(vn::VarName, prefix::VarName)\n\nRemove a prefix from a VarName.\n\njulia> unprefix(@vn(y.x), @vn(y))\nx\n\njulia> unprefix(@vn(y.x.a), @vn(y))\nx.a\n\njulia> unprefix(@vn(y[1].x), @vn(y[1]))\nx\n\njulia> unprefix(@vn(y), @vn(n))\nERROR: ArgumentError: cannot remove prefix n from VarName y\n[...]\n\njulia> unprefix(@vn(y[1]), @vn(y))\nERROR: ArgumentError: optic_to_varname: can only convert Property optics to VarName\n[...]\n\n\n\n\n\n","title":"VarNames.unprefix"},{"category":"function","location":"varname/#VarNames.varname_leaves","page":"VarName","text":"varname_leaves(vn::VarName, val)\n\nReturn an iterator over all varnames that are represented by vn on val.\n\nExamples\n\njulia> using VarNames: varname_leaves\n\njulia> foreach(println, varname_leaves(@vn(x), rand(2)))\nx[1]\nx[2]\n\njulia> foreach(println, varname_leaves(@vn(x[1:2]), rand(2)))\nx[1:2][1]\nx[1:2][2]\n\njulia> x = (y = 1, z = [[2.0], [3.0]]);\n\njulia> foreach(println, varname_leaves(@vn(x), x))\nx.y\nx.z[1][1]\nx.z[2][1]\n\n\n\n\n\n","title":"VarNames.varname_leaves"},{"category":"function","location":"varname/#VarNames.varname_and_value_leaves","page":"VarName","text":"varname_and_value_leaves(vn::VarName, val)\n\nReturn an iterator over all varname-value pairs that are represented by vn on val.\n\nExamples\n\njulia> using VarNames: varname_and_value_leaves\n\njulia> foreach(println, varname_and_value_leaves(@vn(x), 1:2))\n(x[1], 1)\n(x[2], 2)\n\njulia> foreach(println, varname_and_value_leaves(@vn(x[1:2]), 1:2))\n(x[1:2][1], 1)\n(x[1:2][2], 2)\n\njulia> x = (y = 1, z = [[2.0], [3.0]]);\n\njulia> foreach(println, varname_and_value_leaves(@vn(x), x))\n(x.y, 1)\n(x.z[1][1], 2.0)\n(x.z[2][1], 3.0)\n\nThere is also some special handling for certain types:\n\njulia> using LinearAlgebra\n\njulia> x = reshape(1:4, 2, 2);\n\njulia> # `LowerTriangular`\n       foreach(println, varname_and_value_leaves(@vn(x), LowerTriangular(x)))\n(x[1, 1], 1)\n(x[2, 1], 2)\n(x[2, 2], 4)\n\njulia> # `UpperTriangular`\n       foreach(println, varname_and_value_leaves(@vn(x), UpperTriangular(x)))\n(x[1, 1], 1)\n(x[1, 2], 3)\n(x[2, 2], 4)\n\njulia> # `Cholesky` with lower-triangular\n       foreach(println, varname_and_value_leaves(@vn(x), Cholesky([1.0 0.0; 0.0 1.0], 'L', 0)))\n(x.L[1, 1], 1.0)\n(x.L[2, 1], 0.0)\n(x.L[2, 2], 1.0)\n\njulia> # `Cholesky` with upper-triangular\n       foreach(println, varname_and_value_leaves(@vn(x), Cholesky([1.0 0.0; 0.0 1.0], 'U', 0)))\n(x.U[1, 1], 1.0)\n(x.U[1, 2], 0.0)\n(x.U[2, 2], 1.0)\n\n\n\n\n\nvarname_and_value_leaves(container)\n\nReturn an iterator over all varname-value pairs that are represented by container.\n\nThis is the same as varname_and_value_leaves(vn::VarName, x) but over a container containing multiple varnames.\n\nSee also: varname_and_value_leaves(vn::VarName, x).\n\nExamples\n\njulia> using VarNames: varname_and_value_leaves\n\njulia> using OrderedCollections: OrderedDict\n\njulia> # With an `AbstractDict` (we use `OrderedDict` here\n       # to ensure consistent ordering in doctests)\n       dict = OrderedDict(@vn(y) => 1, @vn(z) => [[2.0], [3.0]]);\n\njulia> foreach(println, varname_and_value_leaves(dict))\n(y, 1)\n(z[1][1], 2.0)\n(z[2][1], 3.0)\n\njulia> # With a `NamedTuple`\n       nt = (y = 1, z = [[2.0], [3.0]]);\n\njulia> foreach(println, varname_and_value_leaves(nt))\n(y, 1)\n(z[1][1], 2.0)\n(z[2][1], 3.0)\n\n\n\n\n\n","title":"VarNames.varname_and_value_leaves"},{"category":"function","location":"varname/#VarNames.canview","page":"VarName","text":"canview(optic, container)\n\nReturn true if optic can be used to view container, and false otherwise.\n\nExamples\n\njulia> canview(@opticof(_.a), (a = 1.0, ))\ntrue\n\njulia> canview(@opticof(_.a), (b = 1.0, )) # property `a` does not exist\nfalse\n\njulia> canview(@opticof(_.a[1]), (a = [1.0, 2.0], ))\ntrue\n\njulia> canview(@opticof(_.a[3]), (a = [1.0, 2.0], )) # out of bounds\nfalse\n\n\n\n\n\n","title":"VarNames.canview"},{"category":"function","location":"varname/#VarNames.hasvalue","page":"VarName","text":"hasvalue(vals::NamedTuple, vn::VarName)\nhasvalue(vals::AbstractDict{<:VarName}, vn::VarName)\n\nDetermine whether vals contains a value for a given vn.\n\nExamples\n\nWith x as a NamedTuple:\n\njulia> hasvalue((x = 1.0, ), @vn(x))\ntrue\n\njulia> hasvalue((x = 1.0, ), @vn(x[1]))\nfalse\n\njulia> hasvalue((x = [1.0],), @vn(x))\ntrue\n\njulia> hasvalue((x = [1.0],), @vn(x[1]))\ntrue\n\njulia> hasvalue((x = [1.0],), @vn(x[2]))\nfalse\n\nWith x as a AbstractDict:\n\njulia> hasvalue(Dict(@vn(x) => 1.0, ), @vn(x))\ntrue\n\njulia> hasvalue(Dict(@vn(x) => 1.0, ), @vn(x[1]))\nfalse\n\njulia> hasvalue(Dict(@vn(x) => [1.0]), @vn(x))\ntrue\n\njulia> hasvalue(Dict(@vn(x) => [1.0]), @vn(x[1]))\ntrue\n\njulia> hasvalue(Dict(@vn(x) => [1.0]), @vn(x[2]))\nfalse\n\nIn the AbstractDict case we can also have keys such as v[1]:\n\njulia> vals = Dict(@vn(x[1]) => [1.0,]);\n\njulia> hasvalue(vals, @vn(x[1])) # same as `haskey`\ntrue\n\njulia> hasvalue(vals, @vn(x[1][1])) # different from `haskey`\ntrue\n\njulia> hasvalue(vals, @vn(x[1][2]))\nfalse\n\njulia> hasvalue(vals, @vn(x[2][1]))\nfalse\n\n\n\n\n\n","title":"VarNames.hasvalue"},{"category":"function","location":"varname/#VarNames.getvalue","page":"VarName","text":"getvalue(vals::NamedTuple, vn::VarName)\ngetvalue(vals::AbstractDict{<:VarName}, vn::VarName)\n\nReturn the value(s) in vals represented by vn.\n\nExamples\n\nFor NamedTuple:\n\njulia> vals = (x = [1.0],);\n\njulia> getvalue(vals, @vn(x)) # same as `getindex`\n1-element Vector{Float64}:\n 1.0\n\njulia> getvalue(vals, @vn(x[1])) # different from `getindex`\n1.0\n\njulia> getvalue(vals, @vn(x[2]))\nERROR: x[2] was not found in the NamedTuple provided\n[...]\n\nFor AbstractDict:\n\njulia> vals = Dict(@vn(x) => [1.0]);\n\njulia> getvalue(vals, @vn(x)) # same as `getindex`\n1-element Vector{Float64}:\n 1.0\n\njulia> getvalue(vals, @vn(x[1])) # different from `getindex`\n1.0\n\njulia> getvalue(vals, @vn(x[2]))\nERROR: x[2] was not found in the dictionary provided\n[...]\n\nIn the AbstractDict case we can also have keys such as v[1]:\n\njulia> vals = Dict(@vn(x[1]) => [1.0,]);\n\njulia> getvalue(vals, @vn(x[1])) # same as `getindex`\n1-element Vector{Float64}:\n 1.0\n\njulia> getvalue(vals, @vn(x[1][1])) # different from `getindex`\n1.0\n\njulia> getvalue(vals, @vn(x[1][2]))\nERROR: x[1][2] was not found in the dictionary provided\n[...]\n\njulia> getvalue(vals, @vn(x[2][1]))\nERROR: x[2][1] was not found in the dictionary provided\n[...]\n\njulia> getvalue(vals, @vn(x))\nERROR: x was not found in the dictionary provided\n[...]\n\nDictionaries can present ambiguous cases where the same variable is specified twice at different levels. In such a situation, getvalue attempts to find an exact match, and if that fails it returns the value with the most specific key.\n\nnote: Note\nIt is the user's responsibility to avoid such cases by ensuring that the dictionary passed in does not contain the same value specified multiple times.\n\njulia> vals = Dict(@vn(x) => [[1.0]], @vn(x[1]) => [2.0]);\n\njulia> # Here, the `x[1]` key is not used because `x` is an exact match.\n       getvalue(vals, @vn(x))\n1-element Vector{Vector{Float64}}:\n [1.0]\n\njulia> # Likewise, the `x` key is not used because `x[1]` is an exact match.\n       getvalue(vals, @vn(x[1]))\n1-element Vector{Float64}:\n 2.0\n\njulia> # No exact match, so the most specific key, i.e. `x[1]`, is used.\n       getvalue(vals, @vn(x[1][1]))\n2.0\n\n\n\n\n\n","title":"VarNames.getvalue"},{"category":"function","location":"varname/#VarNames.index_to_dict","page":"VarName","text":"index_to_dict(::Integer)\nindex_to_dict(::AbstractVector{Int})\nindex_to_dict(::UnitRange)\nindex_to_dict(::StepRange)\nindex_to_dict(::Colon)\nindex_to_dict(::ConcretizedSlice{T, Base.OneTo{I}}) where {T, I}\nindex_to_dict(::Tuple)\n\nConvert an index i to a dictionary representation.\n\n\n\n\n\n","title":"VarNames.index_to_dict"},{"category":"function","location":"varname/#VarNames.dict_to_index","page":"VarName","text":"dict_to_index(dict)\ndict_to_index(symbol_val, dict)\n\nConvert a dictionary representation of an index dict to an index.\n\nUsers can extend the functionality of dict_to_index (and hence VarName de/serialisation) by extending this method along with index_to_dict. Specifically, suppose you have a custom index type MyIndexType and you want to be able to de/serialise a VarName containing this index type. You should then implement the following two methods:\n\nVarNames.index_to_dict(i::MyModule.MyIndexType) should return a dictionary representation of the index i. This dictionary must contain the key \"type\", and the corresponding value must be a string that uniquely identifies the index type. Generally, it makes sense to use the name of the type (perhaps prefixed with module qualifiers) as this value to avoid clashes. The remainder of the dictionary can have any structure you like.\nNote that Base.Dict does not guarantee key order, so if you need to preserve key order for fidelity in serialisation, consider returning an OrderedCollections.OrderedDict instead.\nSuppose the value of index_to_dict(i)[\"type\"] is \"MyModule.MyIndexType\". You should then implement the corresponding method VarNames.dict_to_index(::Val{Symbol(\"MyModule.MyIndexType\")}, dict), which should take the dictionary representation as the second argument and return the original MyIndexType object.\n\nTo see an example of this in action, you can look in the the VarNames test suite, which contains a test for serialising OffsetArrays.\n\n\n\n\n\n","title":"VarNames.dict_to_index"},{"category":"function","location":"varname/#VarNames.varname_to_string","page":"VarName","text":"varname_to_string(vn::VarName)\n\nConvert a VarName as a string, via an intermediate dictionary. This differs from string(vn) in that concretised slices are faithfully represented (rather than being pretty-printed as colons).\n\nFor VarNames which index into an array, this function will only work if the indices can be serialised. This is true for all standard Julia index types, but if you are using custom index types, you will need to implement the index_to_dict and dict_to_index methods for those types. See the documentation of dict_to_index for instructions on how to do this.\n\njulia> varname_to_string(@vn(x))\n\"{\\\"optic\\\":{\\\"type\\\":\\\"Iden\\\"},\\\"sym\\\":\\\"x\\\"}\"\n\njulia> varname_to_string(@vn(x.a))\n\"{\\\"optic\\\":{\\\"child\\\":{\\\"type\\\":\\\"Iden\\\"},\\\"field\\\":\\\"a\\\",\\\"type\\\":\\\"Property\\\"},\\\"sym\\\":\\\"x\\\"}\"\n\n\n\n\n\n","title":"VarNames.varname_to_string"},{"category":"function","location":"varname/#VarNames.string_to_varname","page":"VarName","text":"string_to_varname(str::AbstractString)\n\nConvert a string representation of a VarName back to a VarName. The string should have been generated by varname_to_string.\n\n\n\n\n\n","title":"VarNames.string_to_varname"},{"category":"section","location":"#Introduction","page":"Introduction","text":"This package provides two types, VarName and VarNamedTuple, along with associated functionality.\n\nThese types were originally developed for use in Turing.jl, and the code is directly lifted from its dependencies. However, they are general enough to be useful in other contexts and so have been extracted into a separate package.","title":"Introduction"},{"category":"section","location":"#VarName","page":"Introduction","text":"A VarName, most simply, is a data structure which represents an address which can be assigned to. That is to say, it is a representation of an expression which can be used on the left-hand side of an assignment. These include plain symbols (x and y), along with indexed expressions (x[1]), property accesses (x.y), and combinations thereof (x[1].y).","title":"VarName"},{"category":"section","location":"#VarNamedTuple","page":"Introduction","text":"A VarNamedTuple is a mapping from VarNames to values. They are more general than NamedTuples in that they can contain VarNames which are not valid Symbols, such as x[1] or x.y. Unlike Dicts, VarNamedTuples are designed to retain the relationship between sibling VarNames. For example, you can store x[1] and x[2] in a VarNamedTuple, and then access x[1:2], or even x[:] or x (as long as the VarNamedTuple knows that x is a length-2 vector).\n\nVarNamedTuples are also much more performant than Dicts, and in the case where only Symbols are used, they have identical performance to NamedTuples, making them a zero-cost abstraction.","title":"VarNamedTuple"},{"category":"section","location":"vnt/implementation/#Implementation","page":"Implementation","text":"Having discussed what a VarNamedTuple should look like, we now turn our attention to how it is constructed. The core entry point is a function called _setindex_optic!!, which essentially says: 'set the value that can be accessed using this optic, creating it if necessary'. For example,\n\nusing VarNames\nusing VarNames: _setindex_optic!!, AllowAll\n\ncollection = VarNamedTuple()\n_setindex_optic!!(collection, 1.0, @opticof(_.x[1].a), NoTemplate(), AllowAll())\n\nmeans: 'modify this collection such that the value at the path _.x[1].a is set to 1.0, creating any missing structure along the way, with no template provided.' (AllowAll() here is a marker that says that _setindex_optic!! is allowed to create a new key if it doesn't already exist, and also overwrite an old key if it does already exist.)\n\nNotice that, if collection is the top-level VarNamedTuple being used to hold variable values, this is the same as saying \"add the VarName @vn(x[1].a) with value 1.0 to the VarNamedTuple vnt\". Indeed, BangBang.setindex!!(vnt, 1.0, @vn(x[1].a)) directly calls the above.\n\nIf a template is provided, it must be for the entire structure being created, that is, the template should be the shape of vnt.\n\ninfo: Info\nWhen we called templated_setindex!!, we said that the template should be for the top-level symbol, i.e. x. It seems like we are introducing an inconsistency here, since the template above would be for the entire structure. That is why templated_setindex!! does not pass the template as-is; it wraps the template in one level of SkipTemplate{1}(template), which effectively means 'don't use a template for the first level, then use it for the next'.\n\nBecause VarNames have a nested structure, it should not be surprising to find that VarNamedTuples are constructed by recursing into _setindex_optic!!. For an optic like _.x[1].a, the strategy is as follows:\n\nLook at the outermost layer of the optic (.x above).\nIf collection already has a value for that layer (i.e. a property called x), get that sub-value, and call _setindex_optic!! on that sub-value with the rest of the optic ([1].a above).\nIf not, call the function make_leaf, which constructs the necessary sub-structure, using any template provided.\n\nmake_leaf is itself of course also recursive, and is where the bulk of the complicated logic occurs.","title":"Implementation"},{"category":"section","location":"vnt/implementation/#Making-leaves","page":"Implementation","text":"Following on from the explanation above, it follows that make_leaf(value, optic, template) is responsible for creating a new structure s, which already holds value at the path specified by optic. If a template is provided, s should be constructed to match the shape and type of template.\n\nLet's build this up using some simple examples to illustrate the idea, first ignoring any templates.\n\nHere, this is an identity optic, which is the base case. Of course, the value itself (1.0) can be indexed into with the identity optic to give itself. So we can just return the value.\n\nusing VarNames: make_leaf\n\nmake_leaf(1.0, @opticof(_), NoTemplate())\n\nNext, consider a field optic like _.a. To create a structure that holds 1.0 at that path, we can create a VarNamedTuple with a single field a set to 1.0.\n\nmake_leaf(1.0, @opticof(_.a), NoTemplate())\n\nFinally, consider an index optic like _[3]. Since no template is provided, we will need to make a guess: in this case it will be a 1-dimensional GrowableArray, with a size of at least 3.\n\nl = make_leaf(1.0, @opticof(_[3]), NoTemplate())\n# l isa PartialArray, which is quite boring to print, so let's peek inside it.\ntypeof(l.data), l.data, l.mask\n\nConversely, if a template is provided, we'll just use that directly. (But we still have to make sure to set the value at the correct index!)\n\nl = make_leaf(1.0, @opticof(_[3]), zeros(4))\nl.data, l.mask\n\nNow, consider a recursive case, like @opticof(_.a.b). make_leaf needs to create something which can hold 1.0 at the path .a.b.\n\nTo do so, it first has to create the sub-structure that will hold 1.0 at .b, and then it can create an outer structure that holds that sub-structure at .a.\n\nmake_leaf(1.0, @opticof(_.a.b), NoTemplate())\n\nThis should conceptually be fairly understandable. Here is a simplified version of the recursive implementation:\n\n# Let's say `optic` is _[1].a\nfunction make_leaf(leaf_value, optic, template)\n    this_optic, child_optic = split_optic(optic)\n    # ^ _[1]    ^ _.a \n\n    sub_value = make_leaf(leaf_value, child_optic, child(template))\n    # ^ VNT                           ^ _.a        ^ template[1]\n\n    empty_value = create_empty_structure(this_optic, template)\n    # ^ PA                               ^ _[1]\n\n    value = setindex!!(empty_value, this_optic, sub_value)\n    # ^ PA->VNT        ^ PA         ^ _.[1]     ^ VNT\n\n    return value\nend\n\nSome explanatory notes:\n\nThe whole purpose of the function is to ensure that value is something where you can index into with optic to get leaf_value.\nSince sub_value is also created with the same function, that means it must be something you can index into with child_optic to get leaf_value.\nempty_value needs to be something that can hold sub_value at this_optic. We don't yet insert the data. However, to ensure type stability, we should instantiate the PA with the correct element type: in this case that's just typeof(sub_value). (If we don't use the correct element type, the subsequent call to setindex!! will have to change the element type of the PA.)\nvalue is then created by putting sub_value into empty_value at this_optic.\n\ninfo: Info\nRegarding point (3): we haven't yet covered ArrayLikeBlocks (that will be on the next page). If sub_value is something that would be stored as an ArrayLikeBlock, we need to instantiate empty_value with typeof(ArrayLikeBlock(sub_value)) instead of typeof(sub_value), again for type stability reasons. If you are not familiar with this, don't worry about it for now.","title":"Making leaves"},{"category":"section","location":"vnt/implementation/#Multi-indices","page":"Implementation","text":"Multi-indexing, or slices, is where things get a bit more complicated, as it enforces extra constraints that are not present above. To illustrate this, we'll now consider the case where the optic is _[2:3][1].\n\nLogically speaking, this is exactly the same as _[2]. So we should be creating a PartialArray that holds leaf_value at index 2. But on top of that, we also need to make sure that the created structure has at least three indices: otherwise, the index 2:3 would be invalid.\n\n# Let's say `optic` is _[2:3][1]\nfunction make_leaf(leaf_value, optic, template)\n    this_optic, child_optic = split_optic(optic)\n    # ^ _[2:3]  ^ _[1]\n\n    sub_value = make_leaf(leaf_value, child_optic, child(template))\n    # ^ PA (x)                        ^ _[1]       ^ template[2:3]\n\n    empty_value = create_empty_structure(this_optic, template)\n    # ^ PA (y)                           ^ _[2:3]\n\n    value = setindex!!(empty_value, this_optic, sub_value)\n    # ^ PA (z)         ^ PA (y)     ^ _[2:3]    ^ PA (x)   \n\n    return value\nend\n\nThere are three PartialArrays being created here, which are called x, y, and z in the comments above.\n\nThe first point of difference is when creating empty_value: previously, for type stability purposes, we would create the PA with an element type of typeof(sub_value). However, in this case, sub_value is actually a slice of empty_value, and so we can't use its type: we need to use eltype(sub_value) instead.\n\nThe other tricky part is about lengths. Let's work backwards from the last line. z and y will have the same length, since they are related by the setindex!! call. They must be at least of length 3. This is easy to guarantee: when creating y, we either have a template to work with (which must already be of more than length 3), or we can use a GrowableArray and deduce the minimum length from the index 2:3.\n\nHowever, x must also exactly have length 2; otherwise, when setting it into the indices 2:3 of y, an error will occur. This part is more problematic, because the recursive call to make_leaf only ever sees the index 1. If it doesn't have a template to work with, this will error, because it will create a GrowableArray of length 1, which cannot then be set into indices 2:3 of y.\n\n(If it does have a template, we are all good, because the template will be created by indexing into the upper-level template with 2:3, which will give a template of the right size.)\n\nTo solve this, we have two choices:\n\nRecursively pass down information about the expected size of the template; or\nAfter getting sub_value, check if this_index is a multi-index, and if so, expand sub_value to the correct length.\n\nThe current implementation uses the second approach. Note that this is only needed when there is no template provided, and when there is a multi-index.","title":"Multi-indices"},{"category":"section","location":"vnt/implementation/#Detecting-multi-indices","page":"Implementation","text":"This means that in general we need a good way of differentiating between single-element indexing and multi-element indexing. One could iterate over the indices and check which of them are ranges or colons, for example; however, that's not general and doesn't allow for user-defined indices or arbitrary index types. (Of course, if there is no template, then we do fall back on that approach.)\n\nThe solution is this helper function, which is used liberally throughout the VarNamedTuple implementation:\n\nfunction _is_multiindex(template::AbstractArray, ix...; kw...)\n    return ndims(view(template, ix...; kw...)) > 0\nend\n\nwhich works really well across different array types, and is frequently (if not always?) constant propagated.\n\nusing DimensionalData;\nusing InteractiveUtils: @code_warntype;\n\nda = DimArray(randn(2, 3), (X(), :y));\n@code_warntype _is_multiindex(da; a=X(Not(2)), y=2)","title":"Detecting multi-indices"}]
}
